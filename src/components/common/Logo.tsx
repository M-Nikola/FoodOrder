import React from 'react';
import {StyleSheet} from 'react-native';

import LogoImage from '@assets/images/logo.svg';

const Logo = () => <LogoImage style={styles.logo} />;

const styles = StyleSheet.create({
  logo: {},
});

export default Logo;
